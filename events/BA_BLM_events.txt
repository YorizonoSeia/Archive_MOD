add_namespace = BLM_hidden_event
add_namespace = BLM_event

country_event = {
	id = BLM_hidden_event.1
	title = BLM_hidden_event.1.t
	desc = BLM_hidden_event.1.d
	is_triggered_only = yes
	hidden = yes
	trigger = {
		NOT = {
			has_country_flag = BLA_update_falg
		}
	}
	immediate = {
		set_country_flag = BLA_update_falg
		add_offsite_building = {
			type = arms_factory
			level = 1
		}
		add_offsite_building = {
			type = arms_factory
			level = -1
		}
	}
}

country_event = {
	id = BLM_event.1
	title = BLM_event.1.t
	desc = BLM_event.1.d
	is_triggered_only = yes
	
	option = {
		name = BLM_event.1.a
	}
}